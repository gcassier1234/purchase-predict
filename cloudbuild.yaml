options:
  logging: REGIONAL_USER_OWNED_BUCKET

steps:
 - name: "docker.io/library/python:3.13"
   id: CI
   entrypoint: /bin/sh
   args:
   - -c
   - "chmod a+x install.sh && ./install.sh && pytest" 